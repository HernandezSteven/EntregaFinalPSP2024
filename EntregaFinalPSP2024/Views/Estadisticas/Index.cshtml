@model IEnumerable<EntregaFinalPSP2024.Models.Estadistica>

@{
    ViewData["Title"] = "Estadísticas";
}

<div class="container mt-4">
    <h1 class="text-primary">Estadísticas</h1>
    <p class="text-secondary">Administra las estadísticas registradas en el sistema</p>
    <hr />

    <p class="mt-3">
        <a class="btn btn-success" asp-action="Create">
            <i class="bi bi-plus-circle"></i> Crear Nuevo
        </a>
    </p>

    <div class="table-responsive">
        <table class="table table-striped table-bordered align-middle mt-3">
            <thead class="table-dark">
                <tr>
                    <th>@Html.DisplayNameFor(model => model.First().TiempoEmpleado)</th>
                    <th>@Html.DisplayNameFor(model => model.First().Intentos)</th>
                    <th>@Html.DisplayNameFor(model => model.First().Resultado)</th>
                    <th>@Html.DisplayNameFor(model => model.First().FechaRealizacion)</th>
                    <th>@Html.DisplayNameFor(model => model.First().IdEjercicioNavigation)</th>
                    <th>@Html.DisplayNameFor(model => model.First().IdUsuarioNavigation)</th>
                    <th class="text-center">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="7" class="text-center text-muted">No hay estadísticas registradas.</td>
                    </tr>
                }
                else
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.TiempoEmpleado)</td>
                            <td>@Html.DisplayFor(modelItem => item.Intentos)</td>
                            <td>@Html.DisplayFor(modelItem => item.Resultado)</td>
                            <td>@Html.DisplayFor(modelItem => item.FechaRealizacion)</td>
                            <td>@Html.DisplayFor(modelItem => item.IdEjercicioNavigation.Id)</td>
                            <td>@Html.DisplayFor(modelItem => item.IdUsuarioNavigation.Id)</td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@item.Id">
                                        <i class="bi bi-pencil"></i> Editar
                                    </a>
                                    <a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@item.Id">
                                        <i class="bi bi-eye"></i> Detalles
                                    </a>
                                    <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id"
                                       onclick="return confirm('¿Estás seguro de que deseas eliminar este registro?');">
                                        <i class="bi bi-trash"></i> Eliminar
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
